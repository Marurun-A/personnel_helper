<div class="col-md-10 ml-sm-auto col-lg-10 px-4">
   <div class="container-fluid">
    <div class="row">
      <div class="col-md-10">
        <h4>会社詳細</h4>
        <div class="card mb-4 shadow-sm">
          <div class="card-body">
            <div class="row">
              <div class="col-md-4">
                <% if @work.work_details.first.recruitment.image.attached? %>
                 <%= image_tag @work.work_details.first.recruitment.image,size: "250x250" %>
                <% else %>
                 <%= image_tag 'no_image', size: "250x250" %>
                <% end %>
              </div>
              <div class="col-md-3">
                <p class="card-text">
                  会社名：<%= @work.work_details.first.recruitment.recruitment_name %>
                </p>
                <p class="card-text">
                  カナ：<%= @work.work_details.first.recruitment.recruitment_kana %>
                </P>
                <p class="card-text">
                  <% @work.work_details.first.recruitment.tags.each do |tag| %>
                <span>実施業務：<%= tag.tag_name %></span>
                <% end %>
                </P>
                <p class="card-text">
                  受付期間：<%= @work.work_details.first.recruitment.start_date.strftime("%-m月%d日") %> - <%= @work.work_details.first.recruitment.finish_date.strftime("%-m月%d日") %>
                </P>
                <p class="card-text">
                  業務時間：<%= @work.work_details.first.recruitment.start_time.strftime("%H時%M分")  %> - <%= @work.work_details.first.recruitment.finish_time.strftime("%H時%M分")  %>
                </P>
                <p class="card-text">
                  最低業務時間：<%= @work.work_details.first.recruitment.minimum_time.strftime("%-H") %>時間
                </P>
                <p class="card-text">
                  時給：<%= @work.work_details.first.recruitment.hourly_wage%>
                </P>
                <p class="card-text">
                  勤務場所：<%= @work.work_details.first.recruitment.place %>
                </P>
                <p class="card-text">
                  連絡先：<%= @work.work_details.first.recruitment.contact_address %>
                </P>
              </div>
              <div class="col-md-3">
                <p class="card-text">
                業務詳細：<%= @work.work_details.first.recruitment.introduction %>
                </P>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<div class="container-fluid">
  <div class="row">

    <div class="col-md-10 ml-sm-auto col-lg-10 px-4">
      <h4>依頼一覧</h4>
        <div class="table-responsive">
          <table class="table table-striped table-sm">
          <thead>
            <th>希望勤務日時</th>
            <th>希望勤務時間</th>
            <th>返答期限</th>
            <th>リクエストステータス</th>
          </thead>
          <tbody>
              <tr>
                <td>
                  <%= @work.date.strftime("%-m月%-d日") %>  <%= @work.start_time.strftime("%H時%M分") %> - <%= @work.finish_time.strftime("%H時%M分") %>
                </td>
                <td><%= @work.hours.strftime("%-H") if @work.hours %> 時間</td>
                <td><%= @work.response_deadline.strftime("%-m月%-d日") %></td>
                <td><%= I18n.t("enums.work.work_status.#{@work.work_status}") %></td>
              </tr>
          </tbody>
         </table>
        </div>
      </div>
      </div>
    </div>
  </div>
 </div>